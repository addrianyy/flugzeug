[build]
target = "i586-unknown-none.json"

[target.i586-unknown-none]
rustflags = [
    "-C", "link-args=-image-base=0x8000",
    "-C", "relocation-model=static",
]

[unstable]
build-std = ["core", "compiler_builtins"]
